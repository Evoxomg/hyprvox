##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# Example windowrules that are useful

windowrule {
    # Ignore maximize requests from all apps. You'll probably like this.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Fix some dragging issues with XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}

# Hyprland-run windowrule
windowrule {
    name = move-hyprland-run

    match:class = hyprland-run

    move = 20 monitor_h-120
    float = yes
}

# Battle.net fake tray window
#windowrule {
#    name = battlenet-fake-tray
#    match:class = ^(steam_app_default)$
#    match:title = ^$
#    match:xwayland = true

#    workspace = special silent
#    opacity = 0.0 override
#    border_size = 0
#    no_initial_focus = on
#}

# World of Warcraft
windowrule = monitor DP-1, match:title ^(World of Warcraft)$
windowrule = workspace 10 silent, match:title ^(World of Warcraft)$
#windowrule = maximize on, match:title ^(World of Warcraft)$
